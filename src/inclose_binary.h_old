#ifndef INCLOSE_BINARY_H
#define INCLOSE_BINARY_H

#include <Rcpp.h>
#include "aux_functions.h"

// --- Versión 1: Implementación Original ---
List InClose_binary_original(NumericMatrix I,
                             StringVector attrs,
                             bool verbose = false);

// --- Versión 2: Implementación Optimizada con Bitsets ---
List InClose_binary_fast(NumericMatrix I,
                         StringVector attrs,
                         bool verbose = false);

#endif // INCLOSE_BINARY_H
