% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fcaR_helpers.R
\name{iterative_recommender}
\alias{iterative_recommender}
\title{Iterative closure recommender (Simplification Logic)}
\usage{
iterative_recommender(
  fc,
  initial = character(0),
  max_rounds = 4,
  interactive_mode = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{fc}{A \code{FormalContext} with implications already computed.}

\item{initial}{Character vector of attribute names known at the start.}

\item{max_rounds}{Maximum number of question rounds.}

\item{interactive_mode}{If \code{TRUE} and the session is interactive, ask
Yes/No via \code{menu()}; otherwise auto-accept the top suggestion.}

\item{verbose}{Print progress each round.}
}
\value{
Invisibly, a list with \code{query} (final \code{Set}) and
\code{implications} (the last pruned implication set).
}
\description{
Multi-round attribute recommender: at each step computes
\code{closure(S, reduce = TRUE)} (infer + prune the rule-base), then asks
about the still-unknown attribute that appears most often in the LHS of the
\emph{active} implications.
}
