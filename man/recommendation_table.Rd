% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fcaR_helpers.R
\name{recommendation_table}
\alias{recommendation_table}
\title{Pretty table of recommendation scores}
\usage{
recommendation_table(recoms, drop_zero = TRUE, query = NULL)
}
\arguments{
\item{recoms}{Named numeric vector (as returned by \code{$recommend()}).}

\item{drop_zero}{Logical; if \code{TRUE} (default), drop attributes with
score 0 (not implied / not recommended).}

\item{query}{Optional character vector of attributes that were in the original
query. If set, adds a \code{role} column: \code{"query"},
\code{"recommended"}, or \code{"not implied"}.}
}
\value{
A data frame with columns \code{attribute}, \code{score}, and
optionally \code{role}. Rows are ordered by decreasing score, then name.
}
\description{
Turns the named numeric vector from \code{implications$recommend()} into a
sorted data frame that is easy to read in the console or in Quarto.
}
\examples{
\dontrun{
S <- attribute_set(fc, "Romantic", "Slow_burn")
recoms <- fc$implications$recommend(S, attribute_filter = fc$attributes)
recommendation_table(recoms)
}

}
